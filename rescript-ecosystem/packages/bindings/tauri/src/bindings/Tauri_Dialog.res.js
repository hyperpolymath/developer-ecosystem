// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Caml_obj from "rescript/lib/es6/caml_obj.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Option from "@rescript/core/src/Core__Option.res.js";
import * as PluginDialog from "@tauri-apps/plugin-dialog";

async function openSingle(options) {
  var opts;
  if (options !== undefined) {
    var newrecord = Caml_obj.obj_dup(options);
    newrecord.multiple = false;
    opts = newrecord;
  } else {
    opts = {
      multiple: false
    };
  }
  var result = await PluginDialog.open(opts);
  if (!(result == null)) {
    return result[0];
  }
  
}

async function openMultiple(options) {
  var opts;
  if (options !== undefined) {
    var newrecord = Caml_obj.obj_dup(options);
    newrecord.multiple = true;
    opts = newrecord;
  } else {
    opts = {
      multiple: true
    };
  }
  var result = await PluginDialog.open(opts);
  return Core__Option.getOr((result == null) ? undefined : Caml_option.some(result), []);
}

async function openDirectory(options) {
  var opts;
  if (options !== undefined) {
    var newrecord = Caml_obj.obj_dup(options);
    newrecord.directory = true;
    newrecord.multiple = false;
    opts = newrecord;
  } else {
    opts = {
      multiple: false,
      directory: true
    };
  }
  var result = await PluginDialog.open(opts);
  if (!(result == null)) {
    return result[0];
  }
  
}

async function saveFile(options) {
  return Caml_option.nullable_to_opt(await PluginDialog.save(options !== undefined ? Caml_option.valFromOption(options) : undefined));
}

function info(msg, title) {
  return PluginDialog.message(msg, {
              title: title,
              kind: "info"
            });
}

function warning(msg, title) {
  return PluginDialog.message(msg, {
              title: title,
              kind: "warning"
            });
}

function error(msg, title) {
  return PluginDialog.message(msg, {
              title: title,
              kind: "error"
            });
}

var images_extensions = [
  "png",
  "jpg",
  "jpeg",
  "gif",
  "webp",
  "svg",
  "bmp",
  "ico"
];

var images = {
  name: "Images",
  extensions: images_extensions
};

var documents_extensions = [
  "pdf",
  "doc",
  "docx",
  "txt",
  "rtf",
  "odt"
];

var documents = {
  name: "Documents",
  extensions: documents_extensions
};

var audio_extensions = [
  "mp3",
  "wav",
  "ogg",
  "flac",
  "aac",
  "m4a"
];

var audio = {
  name: "Audio",
  extensions: audio_extensions
};

var video_extensions = [
  "mp4",
  "webm",
  "avi",
  "mkv",
  "mov",
  "wmv"
];

var video = {
  name: "Video",
  extensions: video_extensions
};

var json_extensions = ["json"];

var json = {
  name: "JSON",
  extensions: json_extensions
};

var all_extensions = ["*"];

var all = {
  name: "All Files",
  extensions: all_extensions
};

function custom(name, extensions) {
  return {
          name: name,
          extensions: extensions
        };
}

var Filters = {
  images: images,
  documents: documents,
  audio: audio,
  video: video,
  json: json,
  all: all,
  custom: custom
};

function make() {
  return {
          options: {}
        };
}

function title(builder, title$1) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.title = title$1, newrecord)
        };
}

function defaultPath(builder, path) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.defaultPath = path, newrecord)
        };
}

function filter(builder, filter$1) {
  var existing = Core__Option.getOr(builder.options.filters, []);
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.filters = existing.concat([filter$1]), newrecord)
        };
}

function filters(builder, filters$1) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.filters = filters$1, newrecord)
        };
}

function multiple(builder) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.multiple = true, newrecord)
        };
}

function directory(builder) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.directory = true, newrecord)
        };
}

async function pickSingle(builder) {
  return await openSingle(builder.options);
}

async function pickMultiple(builder) {
  return await openMultiple(builder.options);
}

async function pickDirectory(builder) {
  return await openDirectory(builder.options);
}

var OpenDialog = {
  make: make,
  title: title,
  defaultPath: defaultPath,
  filter: filter,
  filters: filters,
  multiple: multiple,
  directory: directory,
  pickSingle: pickSingle,
  pickMultiple: pickMultiple,
  pickDirectory: pickDirectory
};

function make$1() {
  return {
          options: {}
        };
}

function title$1(builder, title$2) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.title = title$2, newrecord)
        };
}

function defaultPath$1(builder, path) {
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.defaultPath = path, newrecord)
        };
}

function filter$1(builder, filter$2) {
  var existing = Core__Option.getOr(builder.options.filters, []);
  var newrecord = Caml_obj.obj_dup(builder.options);
  return {
          options: (newrecord.filters = existing.concat([filter$2]), newrecord)
        };
}

async function pick(builder) {
  return await saveFile(builder.options);
}

var SaveDialog = {
  make: make$1,
  title: title$1,
  defaultPath: defaultPath$1,
  filter: filter$1,
  pick: pick
};

function open_(prim) {
  return PluginDialog.open(prim !== undefined ? Caml_option.valFromOption(prim) : undefined);
}

function save(prim) {
  return PluginDialog.save(prim !== undefined ? Caml_option.valFromOption(prim) : undefined);
}

function message(prim0, prim1) {
  return PluginDialog.message(prim0, prim1 !== undefined ? Caml_option.valFromOption(prim1) : undefined);
}

function confirm(prim0, prim1) {
  return PluginDialog.confirm(prim0, prim1 !== undefined ? Caml_option.valFromOption(prim1) : undefined);
}

function ask(prim0, prim1) {
  return PluginDialog.ask(prim0, prim1 !== undefined ? Caml_option.valFromOption(prim1) : undefined);
}

export {
  open_ ,
  openSingle ,
  openMultiple ,
  openDirectory ,
  save ,
  saveFile ,
  message ,
  info ,
  warning ,
  error ,
  confirm ,
  ask ,
  Filters ,
  OpenDialog ,
  SaveDialog ,
}
/* @tauri-apps/plugin-dialog Not a pure module */
