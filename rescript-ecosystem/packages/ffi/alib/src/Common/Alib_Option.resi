// SPDX-License-Identifier: PMPL-1.0-or-later
// Alib_Option.resi - Extended option utilities

type t<'a> = option<'a>

// =============================================================================
// BASIC COMBINATORS
// =============================================================================

let map: (t<'a>, 'a => 'b) => t<'b>
let flatMap: (t<'a>, 'a => t<'b>) => t<'b>
let bind: (t<'a>, 'a => t<'b>) => t<'b>

// =============================================================================
// EARLY RETURN HELPERS
// =============================================================================

// Guard: Return None if condition is false, Some(()) if true
let guard: bool => t<unit>

// Require: Convert bool to option with value
let require: (bool, 'a) => t<'a>

// =============================================================================
// RECOVERY & FALLBACKS
// =============================================================================

let getWithDefault: (t<'a>, 'a) => 'a
let orElse: (t<'a>, t<'a>) => t<'a>

// =============================================================================
// PREDICATES
// =============================================================================

let isSome: t<'a> => bool
let isNone: t<'a> => bool
let getExn: t<'a> => 'a

// =============================================================================
// CONVERSIONS
// =============================================================================

let toResult: (t<'a>, 'e) => result<'a, 'e>
let fromResult: result<'a, 'e> => t<'a>

// =============================================================================
// SIDE EFFECTS
// =============================================================================

let tap: (t<'a>, 'a => unit) => t<'a>

// =============================================================================
// INFIX OPERATORS
// =============================================================================

module Infix: {
  let \"<$>": ('a => 'b, t<'a>) => t<'b>
  let \"=<<": ('a => t<'b>, t<'a>) => t<'b>
  let \">>=": (t<'a>, 'a => t<'b>) => t<'b>
  let \"<|>": (t<'a>, t<'a>) => t<'a>
}
