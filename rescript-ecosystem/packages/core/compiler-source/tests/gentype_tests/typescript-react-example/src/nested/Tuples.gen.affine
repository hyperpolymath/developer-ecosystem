// SPDX-License-Identifier: MPL-2.0
// Ported via Harvard Engine (Semantic pass)

module Tuples.gen;

/* TypeScript file generated from Tuples.res by genType. */

/* eslint-disable */
/* tslint:disable */

import * as TuplesJS from './Tuples.res.js';

struct coord { [number, number, (undefined | number)];

struct coord2 { [number, number, (null | undefined | number)];

struct person { { readonly name: string; readonly age: number };

struct couple { [person, person];

const testTuple: (param:[number, number]) => number = TuplesJS.testTuple as unknown;

const origin: [number, number, (undefined | number)] = TuplesJS.origin as unknown;

const computeArea: (param:[number, number, (undefined | number)]) => number = TuplesJS.computeArea as unknown;

const computeAreaWithIdent: (param:coord) => number = TuplesJS.computeAreaWithIdent as unknown;

const computeAreaNoConverters: (param:[number, number]) => number = TuplesJS.computeAreaNoConverters as unknown;

const coord2d: <T1,T2,T3>(x:T1, y:T2) => [T1, T2, (undefined | T3)] = TuplesJS.coord2d as unknown;

const getFirstName: (param:couple) => string = TuplesJS.getFirstName as unknown;

const marry: (first:person, second:person) => couple = TuplesJS.marry as unknown;

const changeSecondAge: (param:couple) => couple = TuplesJS.changeSecondAge as unknown;

