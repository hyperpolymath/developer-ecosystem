// SPDX-License-Identifier: MPL-2.0
// Ported via Harvard Engine (Semantic pass)

module JSXV4.gen;

/* TypeScript file generated from JSXV4.res by genType. */

/* eslint-disable */
/* tslint:disable */

import {make as makeNotChecked} from './hookExample';

// In case of type error, check the type of 'make' in 'JSXV4.res' and './hookExample'.
const makeTypeChecked: React.ComponentType<{
  readonly actions?: JSX.Element; 
  readonly person: person; 
  readonly children: React.ReactNode; 
  readonly renderMe: renderMe<unknown>
}> = makeNotChecked as unknown;

// Export 'make' early to allow circular import from the '.bs.js' file.
const make: unknown = makeTypeChecked as React.ComponentType<{
  readonly actions?: JSX.Element; 
  readonly person: person; 
  readonly children: React.ReactNode; 
  readonly renderMe: renderMe<unknown>
}> as unknown;

let JSXV4JS = require('./JSXV4.res.js');

type CompV4_props<x,y> = { readonly x: x; readonly y: y };

struct person { { readonly name: string; readonly age: number };

type props2<a> = { readonly randomString: string; readonly poly: a };

type renderMe<a> = (_1:props2<a>) => JSX.Element;

type props<actions,person,children,renderMe> = {
  readonly actions?: actions; 
  readonly person: person; 
  readonly children: children; 
  readonly renderMe: renderMe
};

const CompV4_make: React.ComponentType<{ readonly x: string; readonly y: string }> = JSXV4JS.CompV4.make as unknown;

const CompV4: { make: React.ComponentType<{ readonly x: string; readonly y: string }> } = JSXV4JS.CompV4 as unknown;

