// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Dict from "@rescript/runtime/lib/es6/Stdlib_Dict.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function string(json) {
  if (typeof json === "string") {
    return {
      TAG: "Ok",
      _0: json
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "Failure",
        _0: "Expected a string",
        _1: json
      }
    };
  }
}

function int(json) {
  if (typeof json !== "number") {
    return {
      TAG: "Error",
      _0: {
        TAG: "Failure",
        _0: "Expected an integer",
        _1: json
      }
    };
  }
  let i = json | 0;
  if (i === json) {
    return {
      TAG: "Ok",
      _0: i
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "Failure",
        _0: "Expected an integer",
        _1: json
      }
    };
  }
}

function float(json) {
  if (typeof json === "number") {
    return {
      TAG: "Ok",
      _0: json
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "Failure",
        _0: "Expected a number",
        _1: json
      }
    };
  }
}

function bool(json) {
  if (typeof json === "boolean") {
    return {
      TAG: "Ok",
      _0: json
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "Failure",
        _0: "Expected a boolean",
        _1: json
      }
    };
  }
}

function $$null($$default) {
  return json => {
    if (json === null) {
      return {
        TAG: "Ok",
        _0: $$default
      };
    } else {
      return {
        TAG: "Error",
        _0: {
          TAG: "Failure",
          _0: "Expected null",
          _1: json
        }
      };
    }
  };
}

function field(name, decoder) {
  return json => {
    if (typeof json !== "object" || json === null || Array.isArray(json)) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Failure",
          _0: "Expected an object",
          _1: json
        }
      };
    }
    let value = json[name];
    if (value === undefined) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Field",
          _0: name,
          _1: {
            TAG: "Failure",
            _0: `Field "` + name + `" not found`,
            _1: json
          }
        }
      };
    }
    let v = decoder(value);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: v._0
      };
    } else {
      return {
        TAG: "Error",
        _0: {
          TAG: "Field",
          _0: name,
          _1: v._0
        }
      };
    }
  };
}

function optionalField(name, decoder) {
  return json => {
    if (typeof json !== "object" || json === null || Array.isArray(json)) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Failure",
          _0: "Expected an object",
          _1: json
        }
      };
    }
    let value = json[name];
    if (value === undefined) {
      return {
        TAG: "Ok",
        _0: undefined
      };
    }
    if (value === null) {
      return {
        TAG: "Ok",
        _0: undefined
      };
    }
    let v = decoder(value);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: Primitive_option.some(v._0)
      };
    } else {
      return {
        TAG: "Error",
        _0: {
          TAG: "Field",
          _0: name,
          _1: v._0
        }
      };
    }
  };
}

function at(path, decoder) {
  return json => {
    let current = {
      contents: json
    };
    let error = {
      contents: undefined
    };
    path.forEach(key => {
      if (!Stdlib_Option.isNone(error.contents)) {
        return;
      }
      let dict = current.contents;
      if (typeof dict === "object" && dict !== null && !Array.isArray(dict)) {
        let v = dict[key];
        if (v !== undefined) {
          current.contents = v;
        } else {
          error.contents = {
            TAG: "Field",
            _0: key,
            _1: {
              TAG: "Failure",
              _0: `Field "` + key + `" not found`,
              _1: current.contents
            }
          };
        }
        return;
      }
      error.contents = {
        TAG: "Failure",
        _0: "Expected an object",
        _1: current.contents
      };
    });
    let e = error.contents;
    if (e !== undefined) {
      return {
        TAG: "Error",
        _0: e
      };
    } else {
      return decoder(current.contents);
    }
  };
}

function array(decoder) {
  return json => {
    if (!Array.isArray(json)) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Failure",
          _0: "Expected an array",
          _1: json
        }
      };
    }
    let results = json.map((item, i) => {
      let v = decoder(item);
      if (v.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: v._0
        };
      } else {
        return {
          TAG: "Error",
          _0: {
            TAG: "Index",
            _0: i,
            _1: v._0
          }
        };
      }
    });
    let firstError = results.find(Stdlib_Result.isError);
    if (firstError !== undefined && firstError.TAG !== "Ok") {
      return {
        TAG: "Error",
        _0: firstError._0
      };
    }
    return {
      TAG: "Ok",
      _0: Stdlib_Array.filterMap(results, r => {
        if (r.TAG === "Ok") {
          return Primitive_option.some(r._0);
        }
      })
    };
  };
}

function index(i, decoder) {
  return json => {
    if (!Array.isArray(json)) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Failure",
          _0: "Expected an array",
          _1: json
        }
      };
    }
    let value = json[i];
    if (value === undefined) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Index",
          _0: i,
          _1: {
            TAG: "Failure",
            _0: `Index ` + i.toString() + ` out of bounds`,
            _1: json
          }
        }
      };
    }
    let v = decoder(value);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: v._0
      };
    } else {
      return {
        TAG: "Error",
        _0: {
          TAG: "Index",
          _0: i,
          _1: v._0
        }
      };
    }
  };
}

function map(decoder, f) {
  return json => {
    let v = decoder(json);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: f(v._0)
      };
    } else {
      return {
        TAG: "Error",
        _0: v._0
      };
    }
  };
}

function map2(f, d1, d2) {
  return json => {
    let match = d1(json);
    let match$1 = d2(json);
    if (match.TAG === "Ok") {
      if (match$1.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: f(match._0, match$1._0)
        };
      } else {
        return {
          TAG: "Error",
          _0: match$1._0
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: match._0
      };
    }
  };
}

function map3(f, d1, d2, d3) {
  return json => {
    let match = d1(json);
    let match$1 = d2(json);
    let match$2 = d3(json);
    if (match.TAG === "Ok") {
      if (match$1.TAG === "Ok") {
        if (match$2.TAG === "Ok") {
          return {
            TAG: "Ok",
            _0: f(match._0, match$1._0, match$2._0)
          };
        } else {
          return {
            TAG: "Error",
            _0: match$2._0
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: match$1._0
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: match._0
      };
    }
  };
}

function map4(f, d1, d2, d3, d4) {
  return json => {
    let match = d1(json);
    let match$1 = d2(json);
    let match$2 = d3(json);
    let match$3 = d4(json);
    if (match.TAG === "Ok") {
      if (match$1.TAG === "Ok") {
        if (match$2.TAG === "Ok") {
          if (match$3.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: f(match._0, match$1._0, match$2._0, match$3._0)
            };
          } else {
            return {
              TAG: "Error",
              _0: match$3._0
            };
          }
        } else {
          return {
            TAG: "Error",
            _0: match$2._0
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: match$1._0
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: match._0
      };
    }
  };
}

function map5(f, d1, d2, d3, d4, d5) {
  return json => {
    let match = d1(json);
    let match$1 = d2(json);
    let match$2 = d3(json);
    let match$3 = d4(json);
    let match$4 = d5(json);
    if (match.TAG === "Ok") {
      if (match$1.TAG === "Ok") {
        if (match$2.TAG === "Ok") {
          if (match$3.TAG === "Ok") {
            if (match$4.TAG === "Ok") {
              return {
                TAG: "Ok",
                _0: f(match._0, match$1._0, match$2._0, match$3._0, match$4._0)
              };
            } else {
              return {
                TAG: "Error",
                _0: match$4._0
              };
            }
          } else {
            return {
              TAG: "Error",
              _0: match$3._0
            };
          }
        } else {
          return {
            TAG: "Error",
            _0: match$2._0
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: match$1._0
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: match._0
      };
    }
  };
}

function andThen(decoder, f) {
  return json => {
    let a = decoder(json);
    if (a.TAG === "Ok") {
      return f(a._0)(json);
    } else {
      return {
        TAG: "Error",
        _0: a._0
      };
    }
  };
}

function oneOf(decoders) {
  return json => {
    let errors = {
      contents: []
    };
    let result = {
      contents: undefined
    };
    decoders.forEach(decoder => {
      if (!Stdlib_Option.isNone(result.contents)) {
        return;
      }
      let v = decoder(json);
      if (v.TAG === "Ok") {
        result.contents = {
          TAG: "Ok",
          _0: v._0
        };
      } else {
        errors.contents = errors.contents.concat([v._0]);
      }
    });
    let r = result.contents;
    if (r !== undefined) {
      return r;
    } else {
      return {
        TAG: "Error",
        _0: {
          TAG: "OneOf",
          _0: errors.contents
        }
      };
    }
  };
}

function optional(decoder) {
  return json => {
    let v = decoder(json);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: Primitive_option.some(v._0)
      };
    } else {
      return {
        TAG: "Ok",
        _0: undefined
      };
    }
  };
}

function nullable(decoder) {
  return json => {
    if (json === null) {
      return {
        TAG: "Ok",
        _0: undefined
      };
    }
    let v = decoder(json);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: Primitive_option.some(v._0)
      };
    } else {
      return {
        TAG: "Error",
        _0: v._0
      };
    }
  };
}

function succeed(value) {
  return _json => ({
    TAG: "Ok",
    _0: value
  });
}

function fail(message) {
  return json => ({
    TAG: "Error",
    _0: {
      TAG: "Failure",
      _0: message,
      _1: json
    }
  });
}

function value(json) {
  return {
    TAG: "Ok",
    _0: json
  };
}

function decodeString(decoder, jsonString) {
  let json;
  try {
    json = JSON.parse(jsonString);
  } catch (exn) {
    return {
      TAG: "Error",
      _0: {
        TAG: "Failure",
        _0: "Invalid JSON",
        _1: null
      }
    };
  }
  return decoder(json);
}

function errorToStringHelp(_error, _context) {
  while (true) {
    let context = _context;
    let error = _error;
    switch (error.TAG) {
      case "Field" :
        _context = context.concat([error._0]);
        _error = error._1;
        continue;
      case "Index" :
        _context = context.concat([`[` + error._0.toString() + `]`]);
        _error = error._1;
        continue;
      case "OneOf" :
        let details = error._0.map(e => errorToStringHelp(e, context));
        return `None of the decoders matched: ` + details.join("; ");
      case "Failure" :
        let path = context.length !== 0 ? context.join(".") + ": " : "";
        return path + error._0;
    }
  };
}

function errorToString(error) {
  return errorToStringHelp(error, []);
}

function toHttpDecoder(decoder) {
  return jsonString => {
    let v = decodeString(decoder, jsonString);
    if (v.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: v._0
      };
    } else {
      return {
        TAG: "Error",
        _0: errorToStringHelp(v._0, [])
      };
    }
  };
}

function dict(valueDecoder) {
  return json => {
    if (typeof json !== "object" || json === null || Array.isArray(json)) {
      return {
        TAG: "Error",
        _0: {
          TAG: "Failure",
          _0: "Expected an object",
          _1: json
        }
      };
    }
    let result = {};
    let error = {
      contents: undefined
    };
    Stdlib_Dict.forEachWithKey(json, (val, key) => {
      if (!Stdlib_Option.isNone(error.contents)) {
        return;
      }
      let v = valueDecoder(val);
      if (v.TAG !== "Ok") {
        error.contents = {
          TAG: "Field",
          _0: key,
          _1: v._0
        };
        return;
      }
      result[key] = v._0;
    });
    let e = error.contents;
    if (e !== undefined) {
      return {
        TAG: "Error",
        _0: e
      };
    } else {
      return {
        TAG: "Ok",
        _0: result
      };
    }
  };
}

export {
  string,
  int,
  float,
  bool,
  $$null,
  field,
  optionalField,
  at,
  array,
  index,
  map,
  map2,
  map3,
  map4,
  map5,
  andThen,
  oneOf,
  optional,
  nullable,
  succeed,
  fail,
  value,
  decodeString,
  errorToString,
  errorToStringHelp,
  toHttpDecoder,
  dict,
}
/* No side effect */
