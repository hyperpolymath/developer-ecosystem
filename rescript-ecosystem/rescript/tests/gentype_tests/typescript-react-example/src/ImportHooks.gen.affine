// SPDX-License-Identifier: MPL-2.0
// Ported via Harvard Engine (Semantic pass)

module ImportHooks.gen;

/* TypeScript file generated from ImportHooks.res by genType. */

/* eslint-disable */
/* tslint:disable */

import {makeRenamed as makeRenamedNotChecked} from './hookExample';

import {foo as fooNotChecked} from './hookExample';

// In case of type error, check the type of 'makeRenamed' in 'ImportHooks.res' and './hookExample'.
const makeRenamedTypeChecked: React.ComponentType<{
  readonly actions?: JSX.Element; 
  readonly person: person; 
  readonly children: React.ReactNode; 
  readonly renderMe: renderMe<unknown>
}> = makeRenamedNotChecked as unknown;

// Export 'makeRenamed' early to allow circular import from the '.bs.js' file.
const makeRenamed: unknown = makeRenamedTypeChecked as React.ComponentType<{
  readonly actions?: JSX.Element; 
  readonly person: person; 
  readonly children: React.ReactNode; 
  readonly renderMe: renderMe<unknown>
}> as unknown;

// In case of type error, check the type of 'foo' in 'ImportHooks.res' and './hookExample'.
const fooTypeChecked: (person:person) => string = fooNotChecked as unknown;

// Export 'foo' early to allow circular import from the '.bs.js' file.
const foo: unknown = fooTypeChecked as (person:person) => string as unknown;

struct person { { readonly name: string; readonly age: number };

type renderMe<a> = React.ComponentType<{ readonly randomString: string; readonly poly: a }>;

type props<actions,person,children,renderMe> = {
  readonly actions?: actions; 
  readonly person: person; 
  readonly children: children; 
  readonly renderMe: renderMe
};

